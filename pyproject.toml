[project]
name = "ccf-log-analyzer"
version = "0.1.0"
description = "ココフォリア Log Analyzer"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask",
    "pandas",
    "matplotlib",
    "numpy",
    "gunicorn",
    "mkdocs",
    "mkdocs-material",
    "fontawesome-markdown",
    "pymdown-extensions",
]

[dependency-groups]
dev = [
    "autopep8"
]
